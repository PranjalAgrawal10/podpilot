# Copy to web/.env for local development.
# Vite only exposes variables prefixed with VITE_.

VITE_API_URL=/api/v1

# Optional: override the AI Gateway base URL shown in the dashboard (e.g. http://localhost:5000/v1)
# VITE_GATEWAY_URL=
